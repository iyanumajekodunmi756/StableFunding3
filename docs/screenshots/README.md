# Submission Screenshots

Drop the three required screenshots here, then update the links in the root
`README.md` (Submission Evidence section) so they point at these files.

| File name | What to capture |
| --- | --- |
| `mobile-responsive-ui.png` | The app at mobile width (e.g., 375px viewport) showing the progress bar, countdown, contribute form, and claim card. Open the Vercel demo, resize to a phone width, screenshot. |
| `ci-pipeline.png` | The GitHub Actions run for the latest commit on `orange-belt-dev` — showing both jobs (`contract-tests` and `frontend`) passing. Visit the repo → **Actions** tab. |
| `test-output.png` | Terminal output of `cargo test` (5 passing) and/or `npm test` (18 passing). |

Tips:

- Mobile: DevTools device toolbar (Chrome/Firefox) → iPhone 14 or similar.
- CI: GitHub → **Actions** → latest run → screenshot the two green jobs (or the job summary page).
- Tests: run `cargo test` and `npm test` in your terminal and screenshot the results.
